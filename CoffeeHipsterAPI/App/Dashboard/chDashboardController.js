"use strict";

angular.module("chDashboard").controller("chDashboardController",
    ['$scope',
        function ($scope) { 

            var init = function () { 

                console.log("chDash loads");
            };

 
            init();
        }
    ]);